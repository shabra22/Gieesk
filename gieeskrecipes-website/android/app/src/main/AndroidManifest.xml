<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.gieesk.recipes" />
            </intent-filter>

            <!-- Android App Links: a tapped gieesk.com link opens the app
                 directly for anyone who has it installed, instead of the
                 browser. Android checks
                 https://gieesk.com/.well-known/assetlinks.json against
                 this app's signing certificate; until the fingerprints in
                 that file are real, verification fails and links simply
                 open in the browser as before. See APP-LINKS.md.

                 Only the apex host is declared: autoVerify covers every
                 host in the filter, and www.gieesk.com redirects to the
                 apex rather than serving assetlinks.json itself, which
                 would fail verification and take the apex down with it.

                 Paths are deliberately narrow — the two kinds of link
                 the app can actually show. The bare homepage is left to
                 the browser. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="gieesk.com" android:pathPrefix="/u/" />
                <data android:scheme="https" android:host="gieesk.com" android:pathPrefix="/recipes/" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
